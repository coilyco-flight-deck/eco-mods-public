[project]
name = "eco-mods-public"
version = "0.1.0"
description = "Public Eco mods (BunWulf family + others) plus the Python toolchain that builds and ships them."
requires-python = ">=3.13"
dependencies = [
    "boto3",
    "jinja2",
    "PyYAML",
    "regex",
]
